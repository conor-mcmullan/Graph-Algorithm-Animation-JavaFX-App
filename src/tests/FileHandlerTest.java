package tests;

import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class FileHandlerTest {

    @BeforeEach
    void setUp() {
    }

    @AfterEach
    void tearDown() {
    }

    @Test
    void test() {
    }

    /*
     * TEST
     *
     * FileHandler
     *  constuctor and method access
     *
     * removedLastChar
     *      empty string
     *      one char long
     *      long string
     *
     * multiRemoveStrings
     *      empty string array
     *      string array
     *
     * displayParsedFile
     *      give valid file path
     *      give invalid file path
     *
     * fileToGraph
     *      simple example graph file, valid file
     *      simple example graph file, invalid file
     *
     * readFile
     *      invalid file path
     *      valid file path - empty file
     *      valid file path - normal file
     *
     * readExampleFile
     *      invalid file path
     *      corrupt file
     *      valid file path - empty file
     *      valid file path - normal file
     *
     * graphToFile
     *      empty graph
     *          invalid file path
     *          valid file path
     *      normal graph
     *          invalid file path
     *          valid file path
     *
     * writeFile
     *      invalid file path
     *      valid file path
     *
     * */


}