package tests;

import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class VertexTest {

    @BeforeEach
    void setUp() {
    }

    @AfterEach
    void tearDown() {
    }

    @Test
    void test() {
    }

    /*
     * TEST
     *
     * Vertex()
     *  contructor + method access
     *      String - name to create plain node
     *      String, Double x & y - VertexCircle for drawing
     *
     * getX() & getY()
     *      String name - constructor getX & getY
     *      String, Double x & y constructor
     *
     * getName()
     *
     * connectToEdge()
     *      valid edge object
     *      invalid edge object
     *
     * connectToVertex()
     *      valid other Vertex
     *      invalid other Vertex
     *
     * getConnectedVertecies()
     *
     * getConnectedEdges()
     *
     * viewConnectedVerticies()
     *      no connections at all
     *      connected Vertex
     *      connected Edges
     *
     *
     * */



}